# Example environment variables for live trading (do not commit real keys)

# Exchange credentials
API_KEY="your_api_key_here"
API_SECRET="your_api_secret_here"
API_PASSWORD="optional_password_if_required"

# General settings
EXCHANGE_ID="binance"
SYMBOL="BTC/USDT"
TIMEFRAME="1h"

# Notes:
# - Copy this file to `.env` and fill in real values locally.
# - Ensure `.env` is gitignored (see .gitignore).
# - Do NOT commit secrets to the repository.
